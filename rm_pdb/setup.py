#!/usr/bin/env python

from distutils.core import setup

setup(name='rm_pdb',
      version='1.0',
      description='A helper module for pdb to debug mutiprocessing children, over simple socket operations.',
      author='BOYPT',
      author_email='pentie@gmail.com',
      py_modules=['rm_pdb'],
      )

